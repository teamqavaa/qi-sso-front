# Base URL of the courses-api Django service (SSO backend uses port 8000)
COURSES_API_URL=http://localhost:8001

# courses-api validates Bearer tokens issued by Digital-Readiness-Lab.
# Copy the SECRET_KEY value from Digital-Readiness-Lab/backend/.env here.
SSO_JWT_SECRET=
